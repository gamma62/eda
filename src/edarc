# System-wide configuration file
# do not edit, save a copy to ~/.eda/ instead

# edarc -- resource config file for eda, with default values and samples
#
# Notes:
# the *_path values must be valid
# boolean can be 0/1 or no/yes or off/on
#

prefix		off
shadow		on
smartindent	on
move_reset	on
case_sensitive	on
autotitle	on

# color palette: 0=bg:black, 1=bg:white
palette		0

# unlink backup file (except the forced backup case)
backup_nokeep	on

# close the shell buffer after "over" command
close_over	yes

# save file with original inode, replace content; transparent for hardlink/symlink
# set to "no", if you want to unlink file before save
save_inode	yes

#indent		space	4
indent		tab	1
tabsize		8

# status line: left:index(%i),filter(%f),lines(%l) right:positions(%p),row/col(%r),hexa(%x)
disp_opts	%i%f%l %r%x

# flags for LOGs by components
#log MAIN2 FH2 CMD2 HIST2 REPL2 TAGS2 SELE2 FILT2 PIPE2 PD2 REC0 UPD0

# find options
find_path	/usr/bin/find
# the '-nH' option is mandatory, '-w' is optional, must be the last
#find_opts	. -type f ( -name '*.[ch]' -o -name '*.sh' -o -name '*.pl' ) -exec egrep -nH -w
find_opts	. -type f ( -name '*.[ch]' ) -exec egrep -nH -w

# ctags settings
tags_file	./tags

# version control systems: toolname and path
#vcstool		cvs	/usr/bin/cvs
#vcstool		svn	/usr/bin/svn
#vcstool		hg	/usr/bin/hg

# make options
make_path	/usr/bin/make
make_opts	-w

sh_path		/bin/bash
diff_path	/usr/bin/diff
ssh_path	/usr/bin/ssh

# some filter examples
#indent -kr  -ts8 -ci8 -di8 -i8 -ip8 -
#indent -kr  -ts8 -ci8 -di8 -i8 -ip8 -l125 -lc125 -
#indent -gnu -ts8 -ci8 -di8 -i8 -ip8 -l125 -lc125 -cp0 -bli0 -npsl -
#a2ps -1 -f8 -R -Egnuc -T4 --stdin=something
#a2ps -1 -f8
#xmllint --format -
